# Coordinates to GPX Converter

This Python Script converts a list of coordinates into a Geocaching.com compatible GPX file.
Place your coordinates into `coords.txt` and run `python3 coords_to_gpx.py`.